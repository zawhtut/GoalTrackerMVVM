<?xml version="1.0" encoding="utf-8" ?>
<Border xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="GoalTrackerMVVM.Controls.ToastNotification"
        x:Name="ToastBorder"
        BackgroundColor="#2C3E50"
        Padding="16,12"
        Margin="20"
        HorizontalOptions="Center"
        VerticalOptions="End"
        Opacity="0"
        IsVisible="False">
    <Border.StrokeShape>
        <RoundRectangle CornerRadius="8" />
    </Border.StrokeShape>
    
    <Border.Shadow>
        <Shadow Brush="Black"
                Opacity="0.3"
                Radius="10"
                Offset="0,4" />
    </Border.Shadow>
    
    <Label x:Name="MessageLabel"
           TextColor="White"
           FontSize="14"
           HorizontalOptions="Center"
           VerticalOptions="Center" />
</Border>
